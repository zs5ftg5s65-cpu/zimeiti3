# 自媒体3.0 Phase 3 变更记录

基线：自媒体二次修复3.zip

本阶段只处理：自媒体运营数据的 Day 绑定、今日作战台任务持久化使用方式，以及今日任务与选题/脚本的关联基础。

## 已完成

1. `Topic` 增加可选 `day?: number`：新增选题默认绑定当前 30 天 Day；旧选题没有该字段时继续作为自由选题保留。
2. `Script` 增加可选 `day?: number`：由今日选题生成的脚本绑定当前 Day；脚本导演手动新建的脚本保持自由脚本，不强制绑定 Day。
3. 今日作战台只读取当前账号+门店+当前 Day 的绑定选题/脚本，不再用 `slice(0, 1)` 拿第一条数据冒充“今日”。
4. 今日作战台增加 Day 对应任务方向展示，明确显示今日主题、拍摄任务、脚本任务和必须产出。
5. 今日选题 AI Prompt 增加当前 Day 的 30 天任务上下文，使后续真实 AI 接入时能直接围绕当天任务生成。
6. 完整脚本 AI Prompt 增加当前 Day 的任务上下文。
7. 今日任务清单的“完成”和“进入模块”拆开：点击任务名称只进入对应模块，不会因为打开任务而误标记完成；勾选图标才改变完成状态。
8. 继续使用已有 `__sm3_war_room_by_scope`，任务状态仍按 `账号+门店+Day` 隔离并持久化。

## 数据兼容

- 没有重命名或删除现有 localStorage key。
- 没有清空历史数据。
- `day` 是新增可选字段，不要求迁移旧数据。
- 旧的自由选题/自由脚本仍保留在选题库/脚本导演中。

## 本阶段未做

- 真实外部 AI API
- 联网热门视频自动采集
- 视频上传与 AI 视频拆解
- 今日脚本自动生成完整逐镜头脚本
- 这些属于后续阶段。
