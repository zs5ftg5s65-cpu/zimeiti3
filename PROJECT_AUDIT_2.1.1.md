# 工作台 2.1.1 二次源码审计

## 结论

2.1 已经比 2.0 完整，但本次复检仍发现 6 类实际问题，其中 3 类会直接影响功能正确性，2 类会造成数据逻辑失真，1 类属于构建环境/依赖风险。

## P0 / P1 问题

### 1. 自媒体模块 ID 问题仍未彻底修复
`navConfig.ts` 传入的 moduleId 已经是 `selfmedia-positioning` 形式，但 `SelfmediaPanel.tsx` 仍通过 `selfmedia-${activeModule}` 查找，导致部分模块无法匹配。

本次改为先标准化：已经带 `selfmedia-` 前缀则直接使用，否则再补前缀。

### 2. 30天计划仍然固定从第1天展示
工作台有 `currentDay`，但自媒体 `DailyPlanView` 内部自己维护 `activeDay=1`，所以从侧边栏进入第15天后，自媒体30天计划仍可能显示第1天。

本次增加 `currentDay` 传递，并同步到 `DailyPlanView`。

### 3. 多账号/门店只是选择器，没有真正隔离
2.1 增加了账号和门店下拉框，但原先选择后并没有把上下文传入数据复盘、历史、素材等数据模块。

本次至少把数据复盘与账号/门店关联，并对真实记录做持久化和上下文过滤；素材库增加持久化。后续若接后端数据库，应继续把所有实体统一增加 `account/store` 主键维度。

### 4. 2.1 的30天成功标准仍然使用了未经验证的播放/涨粉目标
例如“播放量≥500”“涨粉≥5”等属于未经账号基线验证的硬指标，容易让用户误判内容好坏。

本次改为过程和真实数据驱动的成功标准，不再用虚构的播放/涨粉目标作为默认KPI。

### 5. 自媒体选题/脚本中仍存在未经用户确认的经营数字和年限
例如“卖了30年”“每天限量20份”“慢炖40分钟”等。如果不是用户提供的真实信息，不应直接作为发布文案。

本次将这些数字改为“按当天真实情况填写”的模板化表达。

### 6. Vite 配置存在构建风险
`vite.config.ts` 使用了 `@vitejs/plugin-react`，但原 `package.json` 未声明该依赖；同时 ESM 配置直接使用 `__dirname` 存在运行时风险。

本次：
- 增加 `@vitejs/plugin-react`
- 使用 `fileURLToPath(import.meta.url)` 正确生成 `__dirname`

## 数据持久化

2.1 原自媒体数据复盘和素材库主要使用组件 state，刷新后丢失。

本次增加 scopedStorage：
- 数据复盘真实记录
- 素材库元数据
- 当前自媒体账号
- 当前门店

注意：浏览器本地存储只保存元数据/JSON，不等于云端数据库；上传的原始图片/视频文件不会因为保存元数据而永久存储。

## 热门视频真实性

2.1 的“热门方向”不应该被理解为实时热门数据。当前没有平台API，因此页面继续明确显示“需要到平台真实搜索核验”。

## 演示数据

保留的 DEMO_DATA、SAMPLE_MATERIALS、MOCK_HISTORY 等只用于演示界面，不能作为真实运营历史。

## 构建验证

本次进行了本地 import 路径静态扫描：0 个缺失的 `@/` 本地引用。

尝试安装 npm 依赖时因当前执行环境超时，因此本次无法声称 `npm run build` 已成功。交付前请在可联网环境执行：

```bash
npm install
npm run build
```

如果 build 报错，应继续修复到 0 error 后再上线。
