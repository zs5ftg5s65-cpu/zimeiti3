# Phase 9：今日作战台热门参考联动 + Build 可重复安装修复

## 本轮完成
- 今日作战台新增“昨日热门 · 今日参考”区块。
- 优先读取当前账号/门店昨日采集的热门案例，最多显示3条；无昨日数据时按相关度/采集时间兜底。
- 每条案例显示平台、账号、采集日期、已有热度数据（无数据不推测）。
- 已分析案例直接显示“与今日任务”的AI拆解结论。
- 可从作战台进入热门案例库查看AI拆解或原创改造结果。
- 保留热门案例库原有独立入口。
- 新增 `.npmrc`，统一 npm registry 为 npmmirror，并增加安装重试参数，降低 `npm ci` 因网络抖动失败的概率。

## 数据安全
- 未修改、重命名或删除现有 localStorage key。
- 未清空已有热门案例、选题、脚本、任务状态、英语/30天数据。
- 本轮没有新增持久化业务结构。

## Build说明
当前执行环境无法完成依赖下载：`npm ci` 在网络下载阶段超时；因此没有虚报 `npm run build` 通过。
本地/部署环境应先执行 `npm ci`，再执行 `npm run build`。项目现有 `package-lock.json` 与 `package.json` 已保持 npm ci 可解析的版本约束；`.npmrc` 仅改善下载源与重试策略。
